# Changelog

## 1.0.0 - 2026-8-4

- First release: batch-resize selected images against configurable dimension rules into a destination directory
- Persist destination and resize rules across sessions, auto-saving on every change
- Add domain glossary, collision-handling ADR, and batch-resize spec
- Rename project to image-batch-resizer, set app icon, update dependencies, and configure pnpm release-age excludes
